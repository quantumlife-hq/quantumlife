# QuantumLife Configuration
# Copy this to .env and fill in your values

# =========================================
# REQUIRED CONFIGURATION
# =========================================

# Google OAuth (Required for Gmail/Calendar)
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URL=http://localhost:8080/oauth/callback

# =========================================
# OPTIONAL CONFIGURATION
# =========================================

# Azure OpenAI (Optional - for complex AI tasks)
# Leave empty to use only Ollama for local inference
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_KEY=your-api-key
AZURE_OPENAI_DEPLOYMENT=gpt-4

# Plaid (Optional - for Finance integration)
# Get credentials from: https://dashboard.plaid.com/
PLAID_CLIENT_ID=your-plaid-client-id
PLAID_SECRET=your-plaid-secret
PLAID_ENV=sandbox

# Email Delivery (Optional - for sending briefings)
# Use Gmail App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# =========================================
# AI MODELS (Defaults work out of box)
# =========================================

# Local LLM Model (Ollama)
OLLAMA_MODEL=llama3.2

# Embedding Model (Ollama)
OLLAMA_EMBED_MODEL=nomic-embed-text

# =========================================
# AGENT BEHAVIOR (Tune as needed)
# =========================================

# Confidence threshold for autonomous actions (0-1)
# Above this: agent acts without asking
AUTONOMOUS_THRESHOLD=0.9

# Confidence threshold for supervised actions (0-1)  
# Between supervised and autonomous: agent asks first
# Below this: agent just observes
SUPERVISED_THRESHOLD=0.7

# =========================================
# INFRASTRUCTURE (Usually don't change)
# =========================================

# Database path (inside container)
DATABASE_PATH=/data/quantumlife.db

# Qdrant Vector Database
QDRANT_HOST=qdrant
QDRANT_PORT=6334

# Ollama Host
OLLAMA_HOST=http://ollama:11434
